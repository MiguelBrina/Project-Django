# HelloWorld
# como instalar django: pip install django
#como iniciar um projeto django: django-admin startproject "meu_projeto"
#como instalar a venv: python -m venv venv
#E como ativar a venv: venv\Scripts\activate
#Estamos usando Django, python, sqlite3 para o banco de dados e backend  e HTML/CSS para interface gráfica
